// Given a string and a delimiter character. Split the string based on the delimiter and print the list of resulting sub strings.




let str = "This is some, random string, which is, to be, splited! based on, the delimeter";
let delimeter = "!";

let splited_result = str.split(delimeter);

console.log(splited_result);